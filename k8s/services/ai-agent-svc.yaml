apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  namespace: ai-voice
spec:
  selector:
    app: ai-agent
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
