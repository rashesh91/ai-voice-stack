apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freeswitch-pvc
  namespace: ai-voice
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
